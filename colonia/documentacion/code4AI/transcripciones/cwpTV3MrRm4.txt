# RAG Without Text S-Path-RAG
# URL: https://www.youtube.com/watch?v=cwpTV3MrRm4
# Canal: @code4AI
# Segmentos: 330

Hello community. So great that you are back. Yes, today we're going to talk about rag but a rag system you have never seen before. We are going to have a look at a structural topology in a graph space versus a latent manifold in the semantic space. And you might say why now? Now something is happening and if you have seen my last video here can an AI file system unlock intelligence here especially on by Andropic. No, you noticed that we did mention that the main LLM is not learning at all with these file systems. No, if you have the canonical workspace and here the skill MD references script, the task history of this data and everything, this is great. You have everything in MD files, but what about the LLM? And it was such a coincidence quotation marker that Claude Mutos here the Mas is here was leaked here by Entropic and this will be the next generation AI model will be the best model and yes outperforming everything on this planet and you got it. So you notice that Entropic is aware that if you outsource everything to an MD file, you are losing kind of a structural intelligence and therefore they're trying now to yeah leak here and the next AI model will compensate for all of this. But is this true? Can this be true? Because look at this. This has similarities to the classical rack system. No, we have everything outsourced and then we just load it in. We are in context learning. So what could go wrong? I do have a complete YouTube playlist on rack 3.0 agency. So you'll find there everything from hierarch hierarchical reasoning for the graph rack system here to vector of multi- aent system with rack to complete new world model rag and free rag here by Google all the developments here of the last month. But let's talk about this what is currently happening with entropic. And I think my deeper claim is simple that all the language model may fail to reason correctly if we destroy the relational structure where we currently encode knowledge when the text is flattened into chunks. Then we have here a classical rack system. Right. So when the relational structure of our information of our knowledge is not explicitly preserved and put into some MD files, reasoning suddenly becomes now for these new super intelligent EI system like mitos and emergent reconstruction task rather than a direct graph traversal task which is significantly less reliable under some realistic model constraints. What does it mean? Just to give you an idea, no. In a typical old drag system, knowledge is stored here as a separate text snippet, a set of text snippets. No, chunk A, Alice is the CEO of the company X. Chunk B, company X was acquired by the company Y and chunk C, then company Y developed a product set. If they all retrieved independently using now the semantic similarity in a vector space, this means each chunk is selected because it individually resembles here in the semantic similarity of the query. But there's no guarantee at all that they form a coherent reasoning chain because look at this graph. Look here you have if you want here your snippets and you see 1 2 3 4 5 6 seven snippets and now you have to find exactly the logical mapping what snippet belongs to the other snippets. No to connect chunk A to chunk B and chunk C to have a reasoning trace and look at how many possibilities exist in a vector space in a semantic similarity space. So the moment we destroy the original relational structure, we break it apart, the text will preserve to a certain amount the content itself, but often it will destroy the explicit structure. So what is the task of an LLM? become suddenly so much more harder to do this logical reasoning because the LLM must now retrieve the right chunks at the right time. Infer how they connect given its parametric knowledge and the pre-training and then construct the reasoning chain internally in a probability system with an auto reggressive next token prediction system. You see what we are missing out with MRMD files. Now think about the complexity we're going to encounter. We will have combinatorial reasoning but the LLM faces here combinatorial search problem. Think about you have 10 chunks with a rack system. Now LLM now considers which one are relevant in what order under what relationship. But this creates now in the internal computing a combinatoral explosion. So the it seems for us humans, hey this is great if I have a skill MD file or 20 skill MD files and then the experience file and then everything the memory files memory MD everything is outsourced in a natural language file. But this causes some massive problems then for the LLM. How do you want to solve this? How do you want to cut through this this Gordon knot here? How you can do this? No. Well, what we used until now and what we developed was simple. No, in a graph space, we had an explicit structure that reduces the cognitive load on the LLM. It was easy. If we had here a logical chain, a graph structure, a node edge A, B, C, D, it was done. But without this structure, if you put everything back in a semantic space, now the LLM must reconstruct a relationship using pattern matching, which is absolutely unreliable, especially for a complex multi-step reasoning. So therefore, what we really have is a battle, a duality of EI knowledge representation that is going on currently. And this means we operate in two different mathematical spaces. Now we have a graph based reasoning and a sequential modeling for the LLM. With a graph it's easy know we have node and edges entities and relations. But an LLM sees a text as a sequence and reasoning becomes implicit inside the token representation and therefore the token probability distribution. Attention is not comparable to a graph traversal. LLM uses here the attention matrix. No, but attention does not enforce a path validity and it does not guarantee transitive reasoning. So if you compare graph search and sequence modeling, you see where this model will lead us. A graph restricts the search space before the reasoning. But a sequence an LLM a natural language LLM forces the LLM to reconstruct the search base during the reasoning process. And now all the flattening that is happening in the complexity of knowledge transform a structured optimization problem over a graph. Now unfortunately into an unconstrained inference problem over sequences with a latent structure which is strictly harder under the limited computer and attention probabilities to solve. So therefore what a coincidence we have a brand new paper and this is by 26th of March 2026 published. I don't know why they have here on the 5th of March. Never mind. Kia, University of Macau, Shmen University, Hanyang University, Jang University and University of Liverpool. Yeah, they came together and they said you know what we build a new system a semantic aware shortest path retrieval augmented generation for the particular job of multihop knowledge graph. And here we go for the simplest way the question answering. So let's have a look what this is. Now multihop simply means that the answer depends on a chaining several facts together. So instead of asking hey which text chunk looks similar semantically similar. Now a spath rack system that is a complete different uh kind of rag system simply ask does the evidence piece form a meaningful path on a particular domain. So we do have now a shift from a sematic similarity to a structural plausibility and this now becomes an interesting part rag and we will have a deep dive into a little bit of mathematics here is a retrieval and a reasoning framework. So rack is not anymore a rag 3.0. I don't know what you want to call it, but it is really now going nuts. Rack has now a topology aware graph search engine. Plus we will have a semantic guidance for the integration and we do this with a compact latent fusion algorithm. So this is nothing that you know under rack. We have one job enhance the multi-hop question answering over knowledge graph and this paper in my humble opinion has quite some beautiful innovations. It has an explicitly weighted path search strategy, a differentiable and this is the beauty path scoring mechanism that incorporates your classical verifier and injects here a soft latent representation into the LLM and not a language representation. We will talk about an explicit diagnostic to retrieval mapper which can be either rulebased or learned of course and a new mechanism for the transition from soft to discrete graph edits where the rack system ask now here an assistant to add new subgraphs to the main graph and therefore we are also able to have here an additional reinforcement learning that can be here an optimization step on a pure knowledge graph level. So those are some of the new innovations we're going to encounter now in this video. So let's have a look. So beautiful. We have let's say some text, some books or whatever you have some human natural text. Then we transform this into a knowledge graph or maybe you already have a knowledge graph representation. And now we the job is now to extract here from this knowledge graph with millions or billions of nodes and edges here the necessary information for the job that only ask you the LLM to perform. So there is something amazing happening under the hood. I would like to start with a simple example because I always I need here a warm- up phase. You know I need to understand what the hell are we talking about? I want to see the complete structure and then have a deep dive. So I think this is a story that I can tell you. Imagine an LLM is our detective. The detective is trying to solve a complex case. Of course, now the knowledge graph is a massive wall in front of this detective covered with photos of suspects and objects here and places or whatever with some red strings connecting them. Yeah. Can I ask you any eyes to do this? So great. So we have a lot of information right in front of us. Now a standard drag system would just rip 50 random photos off the wall certain subset hand them over to the detective and say just read those I don't know maybe some reports or have a look at these images. No but the detective in here the context window will simply get overwhelmed and confused. It is maybe too much information. So what is happening now? This spath rack system the first look says okay the system looks at a small relevant section of the evidence board at a particular current subgraph lower G and studies how the clues are connected how the red ribbons here are connecting here. What are they focusing in on this particular subgraph? So finding now the red strings it finds here the most logical connection the mapping or logical strings connecting now the clues here that are represented in the images or in the textbooks or whatever we call this the candidate paths that P for paths that are absolutely interesting for the detective for the LLM to solve the job and then there's a filtering and now there's a clever assistance of course it will be another system that scores now the importance of those strings. No, throwing away the irrelevant ones and keep only the absolute best evidence. Yeah. So half selected at a particular time t. Now, instead of forcing the detective to read a massive text report about all of this string in a natural human language, the assistant since it's an AI mathematically compresses now all the evidence and let's say beams [clears throat] or transfers here all this dense data directly into the detective brain and since it's an LLM, we inject now here into the transformer layer a soft latent mixture and we apply a cross attention mechanism. to it. Then then the detective makes here give him all this information a first preliminary guess and says the answer could be a for answer. Beautiful. But if the detective and the companion or whatever they are are not sure they also output now a message and they say hm I need to know here who for this specific um person here who is the boss of this person or where has this person been at 7:00 or whatever you know so the LLM comes back and says I don't have enough information looking now at this information I have to have a diagnostic message sent out to another helper I need now additional information to come to my answer and now the assistant is updating the board gets out interviews people comes back so at 7:00 the person was in this and this place and whatever so the assistant uses now the message to add new specific photos to the evidence board or if it found out oh there was a new alibi no so remove the bad ones so it editing now the graph structure the graph representation no the knowledge graph of itself and then you see we go in a in a loop. No, we just repeat. They do this now round after round up to 10 times if you have here some threshold until the detective is now highly confident it will be a threshold that he has the final answer. So this is the complexity we're talking about you. You see it's a rather simple idea. Now unfortunately if we transfer this to the mathematical representation it gets a tiny bit more interesting. So let's start with the engineering translation. We start with a graph encoding. So you are you have your text you build now a knowledge gra out of the text and you have now a local knowledge graph in front of you and now you have a graph neural network a GNN that processes now this graph with message passing. I have multiple videos on this to create now a dense vector embedding here for every node and edge on your particular subgraph or a set of subgraphs. So this means the network now naturally understands here the topology of the graph and is not just limited by a textual description when a human describes now exactly what it sees here on the graph structure. We have here the mathematical representation of the topology itself available to us. And then we have the path generation and the pruning. As I told you, the system searches now the graph to find here the paths that are important entity A to relation B to entity C. The detective is now thinking about how what could have happened. And now we use here a neural scorer to assign a certain probability to each path. And I show you this is a simple mathematical operation thereby filtering out all the noise and the not so important information and in the end we end up with a cor ced list of selected path that seem to be important. Now comes the interesting part of a soft latent mixture. Instead of converting now all the selected path let's say 20 paths into a text string like A is connected to C via B and B is connected to whatever the system now takes the vector embeddings of those paths and mathematically mixes them into a single continuous tensor in high dimensional space. This is now here our Z context. So suddenly we have a complete different geometrical representation here of those paths and then we have to bring it of course to the LLM. So how we do this? Well, of course we have a gross attention. So the tensor that we have now is passed directly to the LLM transformer layers and the LLM treats now this graph data as a set of a continuous key value structure. I'm going to show you the formula in 2 minutes that its text token my human query can now attend to. So this is what we know and now we have a dual LLM output in the next step. The LLM outputs now after cross calculation the answer a preliminary answer and a reasoning trace. I'm still lacking here detailed information about the entity X. And then we have now the agentic tool use in our rag system. So the system uses now a policy py map to translate now the text message into an actionable let's say a database query to fetch now more nodes more edges specific edges from let's say the internet or a global knowledge graph or whatever sources you have creating now a new updated extended graph network for the next loop for the next run. So you see this is now the step if we have to handle this now in an engineering approach. Here we have it now together. We start with the graph encoding the path generation and the pruning and we create the soft latent mixture. We bring this we inject it here into the LLM with a cross attention mechanism. LLM is thinking about this what it has now information and knowledge creates a dual LLM output. Maybe it needs more information. So now the LLM decides to use some agentic tools to gather more information, calculate something, come back and then it's starting all over again. So you see this rack system as part of rack system is nothing like a classical rack system because the standard graph rack system even the graph rack relies here on a path verbalization. It extracts paw from a graph, turns them into some discrete text or whatever encoding and stuffs them into the LLM context window. But what we want is we kind of want to keep the context window free as long as possible not to stuff all the information into the in context learning methodology. Okay. So to make now the spath rack system learn which paths are actually useful to the lm we have to do yes you guessed it we have to have now a path selection that must be differentiable so we can have our classical back propagation gradients from the lm's final answer all the way back to the graph retriever we want to learn the system something therefore it must be differentiable therefore we have to use now the orus come up with a mathematical trick that we know and this trick is simple but because picking discrete paths is a non-ifferiable step they use here the gumball softmax relaxation formula beautiful what it does simply allows the system to assign our continuous weight to each single path P where we have some noise injected. If you want to have a deep dive have a look at the paper. If not it's a trick. Now what is now interesting is here the cross attention interface itself. Yeah because this is if you want the most interesting innovation that is happening now in this rack system. Now they define here our soft latent mixture our Z context as the weighted sum of the encoded pathway itself. And then instead of adding this to the input embeddings, no they and now this is a simplification. They project here our Z context into the key or onto the key value matrices, our key graph and value graph and then inject them directly into the LLM's multi head attention blocks our soft function that you are familiar with. So our attention has now here the query token for my human query to the LLM and our K graph and value graph representation. So our if you want LLM text query tokens are now directly computing here attention scores against pure graph topological vectors and thereby restricting here the search space saving thousands of tokens and dramatically reducing here hopefully the hallucination probability for our LLM. Now if you really want to understand it from a mathematical side of view this was here a little bit of a simplification but okay if you want it here in detail for every selected path P the graph neural network and the path encoder produce a dense vector representation encoded to the path and if there are let's say 50 selected path of course we have a set of 50 distinct vector representation in a particular mathematical space so the system takes Now the set of these 50 vectors and passes them through some linear projection layers and therefore it creates now here our matrix of keys key graph and the matrix of our values v graph. So each path and this is now important. Each single path has now its own corresponding key value attributes. And now the system uses your particular alpha function never mind contains simply the confidence or if you have a verifier the verify scores itself to scale now the individual path representation themselves. And because our key graph and value graph are matrices representing here individual path, the LLM text token Q token can now independently attend to specific separate paths in the knowledge graph directly. So this is a much more consistent mathematical explanation. Yeah. Okay. This therefore allows here the LLM attention hats, the classical one to scan across all the individual graph paths simultaneously and pick out exactly the topological evidence it needs to answer my human question that I had asking here the LLM to perform a job. So you see this is as I told you we are moving now in the topological space and we are fusing back into the semantic space but we need here the structure of the topological spaces and of the knowledge graph to have a a massive limitation of the search algorithm of restriction of the search space otherwise we would just waste the resources of computing that is just absurd. So then we have as the next step the new they call it the new Socratic graph dialogue. Okay. So what is it? Because the context is inacted as a continuous vector as I just showed you the LLM can now yeah realize another very soft word where there are [clears throat] gaps in this particular vector space. And when the LLM's confidence in a particular answer is low, it therefore as I should admits a diagnostic message telling you this the assistant LLM or AI, hey I need more information about this particular person at this particular point in time about this particular place. So therefore you see the mapping function pi map acts here as an reinforcement learning driven or rulebased agent that now converts this message this diagnostic message either think about sentence transformer as bird models into a mask or into a query. You find the formulas in the paper to expand now the graph for the time period t+1 with this additional information. This is everything we already familiar with. We know how to do this. It is just that this combination we have never seen before. Talking about combination, here you have it now a screenshot and here you have the neural socratic graph dialogue loop. So this is what they call here. But this is exactly what we just went through here in detail. So if you ask me now how to summarize this image reflecting of course what we just talked about and what I presented to you already I would say what is SPF rack system what is it so instead of reading now text human written text the LLM mathematically absorbs here a graph topology that represents our knowledge or our domain knowledge via a cross attention mechanism attempts here to answer it and If it fails and this is important that it fails verbally commands now the graph to expand its borders with another y that provides now additional subgraphs repeating now the process until the mathematical confidence threshold is met and our llm is confident this is the correct answer. You see what an elegant solution but mathematically wow this is quite a nice paper honestly talking about quite a nice paper of course they have to train this model no we were just talking about the operational process when it performs this but in order to perform this you have to train this model and to train the model you know we need a loss function an overall loss function and this is this particular weighted sum so here you have one two three four five different terms. Yeah, you are familiar with the terms if you're a subscriber. So there's nothing specific. But of course here our lambda is here where you can a little bit tune your hyperparameter and this is quite a sensitive topic but otherwise no. So how do we train this? This is important. The graph neural network, the path encoder and the verifier are first pre-trained with a graph-based objective and then the score if you have one in the injection projection layers are now optimized while the language model of course has to remain frozen and we then conduct Telst or a joint fine tuning using here a smaller learning rate for the language model and a larger one for the retrieval modules and optional the model is further refined with a PO based reinforcement. learning using now a task specific reward function. So we are really here I would say at the limit of what you can do here in a rack system. I don't I would not call it a rack system. It is something crazy more complex than a rack system but rack is simply here a keyword that everybody knows and responds to. So let's stay with rack. But this includes also a reinforcement learning of the LLM itself. And this is the beauty what we are missing if you just follow the current entropic ideas. Now talking about the result there are tons of of results in the paper. Just want to show you. Yeah, you see at the end this is here spar rack. You see the the bolt has here the best performance data if you go here for two different benchmarks. Guess what? What a surprise. So it outperforms everything that we know up until now. If you did the the learning in this particular described way we just went through and if you have the perfect data set for the pre-training and and and end. Okay. So what is now interesting that our SPOV rack addresses now the key challenges that we have normally in a knowledge knowledge graph question answering including locating this relevant multihub evidence filtering out LLM plausible but a completely unsupported paths and efficiently presenting here some structured evidence to our LLM so the LLM have a simpler job less hallucination and so on again short summary as part of rack the system ranks semantically weighted paths, suppresses false positives via a verification pipeline, injects our latent paths through the cross attention mechanism, and iteratively refineses the retrieval via model guided graph edits where additional subgraphs are added from the domain graph. And now let's come the paper ends here. Now let's come to my problem here where we started here this video. Does the spot rack solve your my flattening to text problem that we see with entropic? And guess what? Why I had chosen this particular paper. Yes, this is the exact core problem that spuff rag was invented to solve. What a coincidence. Now, so because when we use standard graph rag on naive knowledge graph augmented LLMs, we have to translate the graph back into text so the LLM can read it. And for multihop reasoning, the standard system generates long verbalized strings like this one. And this is for sure a measure for disaster. So beautiful. And the authors even point out that the two fatal flaws with this flattening approach here is now if you want cured by this new rack system because what we had was the token context bloat. It wasted massive amount of token in from the token budget because if the system retrieves 50 possible paths and this is the low end verbalizing all these 50 possible path in all their complexity fills up the context window with redundant noisy text and maybe some of the paths are contra against other paths or neglect other path or I don't know you know this is something you do not want to have here in your prompt structure. And of course the topological blindness. No, LLMs are autogressive next token predictors. They process the text linearly. And if you have a single token or multi-token prediction, it doesn't matter. It's linear. So when you flatten a highly interconnected knowledge that [snorts] is in a graph representation now in a linear text into a linear text representation, the LLM loses here the explicit structural hierarchy and the logical connectivity of the knowledge of the data in this linear text human language representation. So therefore, I'm not a fan of the markdown files because we make it the life so much harder for the LLM to find our logical connections. But spre solves all of those problems that I just showed you because instead of translating the graph into English, it translates the graph into some continuous mathematical vectors or that context and directly fuses them into the brain with a cross transformation here transformer layers and by using it a path encoder and projecting it directly into the keyon values. Just showed you this in the in the detailed mathematical explanation for the LLM cross attention blocks. The LLM attention heads can then perform the classical mathematical looks across the graph topology directly in the vector space. So you make life for the eyes so much easier if you have here not only a textual MD description but if you go for a structural description here in a topology that helps you the LLM to understand the complexity of multihop reasoning stuff. Great. What is the result? Zero token bloat. Look at this. The graph data does not consume the LLM standard text token window and zero topological loss because the graph neural network preserves the multihop relation as geometric distances in their latent space meaning that the LLM can feel okay detect the structure of the data rather than trying to read it in a linear fashion and then guess what is their topology. So you see this is the reason why I think graph data topological representations should augment our textual complexity and I understand for entropic it is so much easier to outsource everything to a markdown file but for the AI itself for the complexity that the LLM is now encountering this will not solve the problems that we are facing currently. So there you have it, my personal reflection on this beautiful paper. Please go read this paper. There's a lot of more detailed mathematics. I just skipped to give you the overall picture here, the main results, the main insights into the paper and hopefully explain it here with this example. I hope you enjoyed it. I hope there was some additional information for you. Would be great to see you in my next video.