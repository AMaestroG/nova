# Cognitive AI New Solution
# URL: https://www.youtube.com/watch?v=OGjzEF5SoGs
# Canal: @code4AI
# Segmentos: 698

Hello community. So great that you are back. Let's talk about the latest in AI research. Here we go. And we built here something absolutely crazy because you remember we had a look here at our AI in the core and the AI harness and today we allowed that now the core learns from the harness. This is something we haven't seen before. So let's start. Now you remember in my last video we were talking about Milky Way. We were predicting the future and we were building something external memory harness elements for the chronological elements that happened. And today today we focus here on this two study published April 17, 2026. Learning to reason with insight for informal tier improving and an agent reinforcement learning with an agent verifier. City University of Hong Kong, Chinua University [snorts] and here we are Fudang University, University of Hong Kong bite seed and a lot of other very intelligent people. So if you read both paper, I think they are strictly anchored in a deterministic formal but also in an informal mathematical logic. They tackle a closed system reasoning where the truth value here is immutable and the primary bottleneck is the deep logical deduction rather than any temporal uncertainty like we had a look in my last video on Milky Way. So let's go into media. What is here the unifying paradigm? It is about here a duality between a generator AI and a verifier agent. And if you combine those two paper, it bridges here kind of the fundamental gap that we have in the current test time scaling and in the reinforcement learning for our LMS because in both we do have either an actor critique or a generator verifier divergence. And these two two new papers propose something really interesting. Now currently the mall fail because the actor generates some flawed step-by-step logic without a higher level planning and on the other side the critique it verifies those step using some superficial pattern matching rather than any grounded logical tracing or any support by lean for other numerical solver. So let's have a look the deep side insight theorem. The first paper provides a mathematically sound methodology for the actor agent forcing here a top-down insight and hierarchical sketching. I will show you this in detail. While for the second paper it provides a methodology for the critique agent forcing here a bottomup programmatic verification either we go Python or C++ and therefore we will achieve in total a birectional logic consistency check that will perform real nice. So if I got your interest those are the two papers of today. Let's start with the first one with an informal tier improving. So what they tell us we have two new innovation we build a new hierarchical data set that we call a deep insight theorem and then we develop a new supervised fine-tuning framework and now it happening and now finally we can train the LLM at the core of all of this was a more insightful reasoning by explicitly learning to identify some core techniques. Now if you're not really familiar we have the formal automated theorem improver you know there's no lean lean for and so on and they often face a substantial gap between the human mathematical exposition and their formal language and then we have something we call an informal improving that generates here mathematical or physical or whatever you have proof logical proofs directly in a natural English human language and standard mathematical notation like you have in latte which aligns well with a typical LLM. pre-training with their vocabulary with the tokenizer and everything we can use here beautifully. If you want to have an introduction to the topic I would highly recommend this paper by Tensen and Shankai Jaong University from June 2025. Now the authors tell us that the mathematical reality of how an LLM fails at complex reasoning is simple. It is token level entropy spike. When a normal LLM writes a proof, it cruises smoothly through a basic logical connection. low entropy, high confidence. But the very moment the proof requires a massive conceptual leap, which they call a core technique, the model's internal uncertainty, the model's entropy spikes. So this means because a complex proof requires a sequence of K core techniques, the overall probability of generating now a valid proof decays exponentially. So if you do not explicitly train an AI model to isolate, retrieve and lock in those core techniques very early in the training phase, the probability of it magically guessing your all K techniques in the correct sequence during a rambling chain of sort process this is approaching zero probability. So artists came up with a solution. They said what we need is a hierarchical probability decomposition. So in order to train the model to map a question directly to a proof is more or less nonsense. We need a four stage hierarchical structure. Stage one is the question itself. Then we have to train for the core techniques. So the model must explicitly name the pivotal tools for example required no like construct a nested open set and then comes the sketch of a proof. So the model must now write a highlevel enumerated outline of the logic connecting now all those core techniques to the main question and only if we have this then we have the full proof only now is the model allowed to execute the dense low-level latte mathematical deduction. So if you want to write this in a mathematical way, a monolithic generation pi data is transformed into a conditional change and you see here the very first time is now we are forcing more or less the model to maximize our pi data first. This means identifying the insights before doing the mathematic. So this means the framework prevents now the model from wandering blindly into some logical dead ends if the model is just trying out some mathematical proof solution. The way they did this here is they said decided okay we need a particular data set engineering yeah because we need here specific data set they call this the deep inside theorem corpus and they took about 100,000 highquality theorem improved mathematical pairs and used here a deepseek or one to don't smile to reverse engineer them and they annotated the data to extract three distinct classes of insight and please note this is a later on a kind of a limitation because we go only with three distinct clauses of insight. The first is the construction no introduce some auxiliary objects no define a sequence then we have the theorem call invoking known external lemas and then we have the mathematical transformation recasting the problem into a new framework. Let's say we are shifting from the number theory to the mathematical topology framework. So they force the data to include also some analytical preamble like hey let's analyze now the condition ABC or this would suggest now the following. So you see some if you want logical remarks and this kind of acts as a localized macro chain of sort giving here hopefully the model the textual runway to deduce now the insights and then if you have the data set we have to train this model and they went here with a supervised fine-tuning a progressive multi-stage supervised fine-tuning and you know you cannot simply feed a complex four-part hierarchical data to a base LLM and really expected to learn this. Now, so you know what we have to do? We have to apply curriculum learning. So in order to teach a novice AI the highle abstraction of a mathematical topology framework before they know how to write basic algebra they will simply fail. So therefore the researcher designed a curriculum that perfect mimics here the human learning process. Stage one was if you want the apprentice. No, the model is fine-tuned on standard question proof pairs to learn here the basic syntax. Stage two was if you want the journey manner the model is now trained to generate a sketch first and then the proof. Learn to follow a stepwise outline and internalize here the structural abstraction. And then finally stage three the expert level. The model is now trained on the full hierarchy. learn to leap directly from the raw question to the abstract core techniques and use those to govern now the entire downstream generation. So you see by splitting here our supervised fine tuning process in three progressive stages the learning objectives are kind of decoupled. Yeah. So the model learns at first the syntax also like pre-training a language then the structure and then the insight. And you can really apply this here to a mathematical proof generation. Here the advantage is mathematical is very logic. So we have no problem at all to really map to these three structures. And now from preventing your neural network from merely memorizing here the surface level text of the proof. It is forcing it to actually learn here with this curriculum learning the underlying mapping from the problem state to the required set of techniques. Okay. If you want to see this here in a little screenshot here as here done by the ores again we have the deep inside theoreium constructing here the inside structure we have the base data set 121,000 high quality uh international mathematical olympiad level informal proofs then we have the core techniques the proof sketching and the full proof and then we have the progressive multi-stage supervised fine-tuning with this curriculum training that mimics here the human learning journey here apprentice joining me an expert beautiful now let's come to the result this is now interesting because they argue here a little bit hopeful I would argue now so okay let's just look here the first three lines the Q and 2.5 from a 1.5 billion a tiny tiny to a 3 billion to a 7 billion so what do we have here we have here three benchmark beautiful and the average here in the last column let's look just at the average so we have here the base nine if you want the baseline is what the baseline represents the model our 1.2 1.5 billion trained via a standard supervised tuning on the raw theorem proof pairs. So this is if you want this is here the baseline that we want here to have to achieve a better result in those. Now the three-stage is here the full-fledged methodology I just presented to you and you see for the 1.5 billion model we go from 15.94 to 17.77. So honestly I personally would have expected a much more significant jump but okay this is the data by the way how do we generate this data this is here done here by R1 deep R1 acting here as a judge and the judge has the following schema the logical validity is 40% of the weights the completeness is 30% of the weights and the clarity is the rest so this is we how we get here this performance data Now the argumentation is look we have here a 1.5 billion where we have with our beautiful training now 17.7 and this is already close to the base model of the next LLM that is twice the size and yeah I I see it now the 3B has a base model on average from 18.8 so instead of the jump from 15.9 to 18.8 8 is not a jump from 17.7 to 18.8. Is this here um a significant improvement? Well, it depends on yeah on the one hand I see that it is improvement. Yeah. But yeah, look at the 7B. Here we have 22.28 28 and then with all of this additional supervised finetuning and the training and the data set generation we just jump on average from 22.2 to 25.8 8. Now I know if you are in an international uh mathematical olympiad if you have two three four p points more is significant absolutely but for the amount of complexity that we build now into the training this new LLM and although it's just a 7B model or a 3B model I expected here the jump to be a little bit more significant. If you disagree with me, leave me a comment beautifully. But this is now here from the first paper, the result. So you know exactly what you can expect if you work with a 1.5 billion, a 3 billion or a 7 billion model QN 2.5 not the optimum. If you go now with a current 3.6 QN, maybe you even get a better performance. Great. Now to the insights. Now what are the insights? The insights are simple. Abstraction is really a surrogate for scaling. Yeah. Because the experiment yielded you a profound insight regarding the model size. By structuring here the reasoning process like we just discussed through this expert curriculum. Ultra small models like I showed you the 1.5B achieved massive gains in the reasoning benchmarks. So this means teaching a small model how to plan yields better reasoning than simply scaling up here a stupid model to 70 billion parameters and not apply this particular curriculum learning. Now of course there are limitations. No you remember supervised fine-tuning here does not allow the model to actively search here for insight via a trial and error reinforcement learning. This means a very weak generalization. We will have an out of distribution error. Yeah, if you have a error somewhere of course plus it will inherit here the exact same epistemic blind spot like as I told you we used the R1 latent space now and if R1 has here a blind spot for a specific type of some algebraic geometry all the other data set that we generated with the help of R1 will have exactly this blind spot. As I told you, we have here the mathematical insight into exactly three buckets that I told you. The construction, the call and the transformation. While this might be useful for some simple problems, yeah, the training regularization, if you go out and have here some real world problems, no, you know, you're not going to survive with just three buckets. Now, so the real world advanced mathematics here is highly more fluid, highly more complex. So this simple construct will not be your optimal configuration for your advanced reasoning. Anyway, in my last video with Milky Way, I showed you we have some learning and we save this new learning methodology outside in the if you want EI harness. Now we do exactly the opposite. We have here supervised fine-tuning and reinforcement learning. So we feed the Laurent context, the Laurent methodology back to the core LLM and finally we allow our LLM to improve its reasoning capabilities. So this means the deep insight here improves the DI reasoning failure that we encounter today is not necessarily a lack of a computational power but rather a failure of some cognitive sequencing. So the pre-training optimization of particular data set was not optimal. But as I told you we do also have here a second paper here scaling the reward model with a gentic verifier. Now if you want have a look yourself if not I give you a very short overview because what I'm interested in is the combination. So the combination of both papers the inside theorem and the agent RL proves now that scaling our LLM capability in in exact science mathematic theoretical physics cannot be achieved by increasing the parameter count or you increase the context length alone of an LLM. This will not help you. It will really require strict structural determinism. So the network must be forced to project some abstract insight prior to the generation as I showed you here with this planning and forced to execute some non-ifferiable deterministic verification prior to the sequence termination. They show here in this second study that they introduce here complimentary forward and backward agents if you want. This is the main new innovation. So one traces the solutions from the premises to the conclusion and the other agent rejects here the conclusion against their underlying premises. So if you want to have a birectional process with the forward and backward agent enabling now a comprehensive and hopefully reliable and let's cross our finger interpretable assessment of the solution. So you see what we're doing here. We are checking we are checking backwards and then we are checking again the complete structure clear indication nobody trusts here the LLM uh pure statistical model and auto reggressive next token prediction model we have now more and more layers here just making sure it is checked from A to Z but let's stay here with if we combine the inside of both papers because yeah reasoning is advancing here through either the test time scaling that you know this means generating dozens of potential answer and we take the best of n during the inference run or iteratively we try to optimize and refine here some structures no but in anyway our TTS our test time scaling is bottlenecked by and you know it it's the reward model no the verifier itself and normally the standard verifiers output here an uninterr interpretable scalar score 0.87 but what does it really mean if you have a complex mathematical proven what does 0.87 mean lma 4.3 or theorem 2.8 eight. So, and in addition suffers here for massive error propagation. Yeah. So, those verifiers are easily fooled here by proofs that look like structurally plausible but it can contain massive fatal mathematical flaws. So this is not the way forward and therefore the authors of the second paper our agent oral tell now hey to replace the static easily tricked kyla judges here we need a multi-turn tool augmented and yes we again here in the harness deliverable agent that actively interrogates now candidate solutions so what you have again is here if you want a controlling agent that says hey I don't believe you anything we have now mult multi-turn conversation. We will have tool augmented verification and maybe then you get a go and it can escalate here to the next level. And they prove that increasing here the parameter size here of your specific agent here of a standard reward model is just highly inefficient. It doesn't improve the general solution. Instead, autos found an active researcher must try here to build verifiers, very specific verifiers that utilize here the knowledgebased external grounding. So this means either you have a Python code execution, you have any uh numerical solver. You just want to make sure that you have a sequence of text here or a sequence of mathematical structure somehow bring it over to a Python code to a lean force construct whatever you have and mathematically prove here deterministic here the generator's logic and you should do this rather than just relying here on some LLM's internal latent space that is more or less simply guessing if the answer is right or not. This is not good enough in medicine, not good enough in finance, not good enough in mathematics, not good enough in physics. And you get the idea. So we are building here really unbelievable structures just to check and check again and doublech check everything. So with our birectional tool augmented verification that the artist proposed now you immediately understand here instead of a simple single forward pass classification we have now multi-turn cognitive looping between two distinct modules and at first let's make it easy you have a forward agent if you want to call it sufficient checking and then you have a background agent for the necessity checking yeah now the job of the forward agent is simple it traces here the candidates's logic step by step from the initial premises that we have to the conclusion. Let's say ensure that no logical leaps were made. Everything is beautiful, coherent here. No, everything is smooth. You got the idea. The backward agents simple as we as we said it reverse engineers the logic starting from the finally generated answer working backward to ensure it actually satisfied here the strict constraint of the original prompt at the very beginning of the task. And if both deliver here success flag beautiful. So you see in addition to those agent we need here deterministic tool grounding. So both agents let's say are integrated with a bidon compiler or whatever lean for it really doesn't matter but you have to have a deterministic system that you really can calculate deterministically and when they encounter complex algebraic claim in the text they do not trust the LLM. I mean this is this is really now the essence here of all the AI researcher. You under no circumstance can trust anything that comes out from the LRM. What they say you have to write now a Python script mapping here whatever comes out of the LRM into a way that we can execute it. We can calculate it. We have a mathematical formulism. We have a coding framework and then we use this deterministic output to strictly validate the claim. what the LLM claimed is now the answer. I mean we have here two and three double looping just to make sure that whatever comes out of the LLM is double and triple checked and this is just crazy because we waste now I don't know more than half of the complete energy or the complete time just for verification and double verification. Now what is interesting from a theoretical point of view is of course the training process of this itself. Now as I told you we have like in the first paper a supervised fine tuning to teach the model here the multi-stage plan plan validate and verdict and then of course you know this is our standard GRPO reinforcement learning methodology the group relative policy optimization to teach the verifier how to autonomously interle its internal reasoning with some external tool use. Let's go with Python to maximize the verification accuracy. So we do not believe anymore in LLM as a judge. Even if you have I don't know a GPD 6 or 7 or 8. We don't trust this. You have to have an external [clears throat] tool use a Python and deterministic tool that is verifiable that is debugable and that is deterministic. If you want to have here the complete explanation that we just went through in the last minutes here in one little graph, this is it. Okay, beautiful. So both studies together, I think there's this beauty and this is what I felt. This was the feeling where I said hey I have to show you both studies here at the same time because I think if we merge now the deep inside theoreium here and the agent RL to achieve here the ultimate mathematical complete cognitive architecture for AI reasoning. Unfortunately today I have to say because those two papers perfectly fit here or complement each other although we have to have a extreme set of agents and interwoven agent checking and so on. Now the first as I already told you at the beginning of this video we have here the divergence of here the generator and the verifier agent. Huh? Now think about it. The deep inside the first paper proves that the actor or the generator agent will fail if it blindly generates text from left to right. If you have an auto reggressive next token prediction, forget about it. So it solves this by forcing now the actor to engage here in a top- down hierarchical abstraction extracting here core insights like we discussed at the beginning of this video and then sketching out a plan before doing anything mathematical proof reading or whatsoever. However, even the best actor occasionally will hallucinate. So therefore we have a backup solution and this is where the second paper comes in. And now we have here the critique of the verifier agent. Huh? It takes up if you want a hierarchical proof generated by the actor and subjects it to bottomup deterministic interrogation using now what we just learned the forward backward tracing and the Python code deterministic validation. So I think those two papers aren't they beautiful? Yeah. If if you read multiple papers in parallel sometimes your brain tells you hey wait a minute I can combine this. This is beautiful. Look at this. They really complement each other and you can build something much more powerful if you just integrate one methodology into the other one. Or let's go with a geometric interpretation. Let's say you want to see this as a cartisian logical system. Then simple just think about here a multi-dimensional reasoning grid and simplest case is two-dimensional. So we have an x and a y axis. No and however you want to distribute it. Let's say here one of the axis is for the depth. Yeah, the deep inside theorem maps here the vertical dep of the logic following here the insight to sketch to prove and on the other orthogonal axis. Now we have our reinforcement learning methodology that maps the horizon flow of logic here where we have here tool integration and everything. So what is the conclusion here of both papers and please have and read both papers it's I know it's sometimes but you learn so much about it I think one of them is the deep inside theorem restricts here you actor search space so that it doesn't get lost in the exponential probability decay as I showed you the second paper shows us that here with the agent reinforcement learning methodology this strictly grounds yet a critique agent so it doesn't suffer from the confirmation bias that we talked about and synthesizing them now kind of dictates here the next generation of AI models now and they cannot be simple a single monolithic model again it would be so much more powerful if you could have an orchestrated system where and let's go with this example a strategically constrained hierarchical generator passes its output to an unforgiving code wielding birectional interrogator agent. Yeah, just utilize these two papers. Build the code, ask code, whatever you like to go with and build the system and compared. I think this is such a beautiful insight. Sometimes reading those papers, you can really improve your system. Now you might say, is this the best topological construct that we can think about? No, not at all. But unfortunately I have to confess this is if you want the safety and if you want the trustworthiness of AI model this is more or less the best that we can come up with currently. I hope you had a little bit of fun. You enjoyed it. I hope to see you in my next