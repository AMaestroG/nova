# Words Instead of Weights HERA
# URL: https://www.youtube.com/watch?v=ziaNZCtjuPI
# Canal: @code4AI
# Segmentos: 313

Hello community. Let's talk today about a very latest in rag. Okay, of course we are multi- aent but we also notice here a real shift in the research. We're moving away from a continuous parameter space and an optimization where we look at the LLM weight update the tensor updates and now as I showed you already in my last videos more and more we are moving here to the discrete system space outside of the LLM. the architect just the topology of the agentic network the prompt optimization and the code optimization for the complete system. So if you remember this was my visualization where I showed you here the core alarm at the center and then around it the AI harness or the harness sphere with everything from a standard rack pipeline to a graph rack to a database cluster API backbone lean for formal reasoning engines and now we come have the very latest in rack and of course it is a multi- aent rack system that is selfoptimizing and self-developing Of course, I mean what else? No. Now you see that in today's paper, we will provide some blueprints for training free system optimization. And training free means the LLM is not trained at all. We have frozen weights. And you might say, hey, but already in your last video also you showed something that is a training free system optimization when [snorts] we looked at meter harness and we looked also at memory optimization. Absolutely. So let's have a look for complex reasoning or for multi- aent coordination what we going to introduce today is not a numerical gradient but today we introduce here the topic of a semantic gradient and if you are like I say what is a semantic gradient well turns out instead of using scalar rewards to update here the agent policies and a typical reinforcement learning optimization Now we use an agent here let's call it the orchestration agent to general and now hold on to your socks some natural language insight and we comparing your successful and failed trajectories and then we take these natural language insights and store those in kind of a database or just call it an experience library to dynamically root your future queries. So say wait a minute. So we go away from a pure mathematical optimization theorem and we now build an optimization idea based on natural language insights. And we have here this complete smeared out interpretation of how you can interpret a human language statement. And this is what is now our optimization paradigm. Exactly. So welcome. This is here Virginia Tech and we have from April 1st 2026 experience as a compass a multi- aent rag system with an evolving orchestration and agent prompts. They call their system Hera hierarchical evolution of multi-agentre and it optimizes now very specific agent topologies complete topologies through a reward guide. It's sampling and experience accumulation and now you know it is not a numerical reward but it will be a natural language optimization theory. Okay. So what is here the core innovation of today's paper a training frame the frozen weights here of our core LLM hierarchical framework that dynamically will evolve and it will take care about two things in parallel about the multi- aent orchestration topology how do we build our topology what if we have this orchestration mastermind and the individual agent prompt so if you want prompt and context engineering using here something like maybe you know it expectation maximalization like process design. So it was inspired here as did the artist tell us by GRPO a group relative policy optimization from reinforcement learning but now they lifted here the complete policy optimization and I will provide further details here to the topology level of the agent itself. So therefore for a given query here samples now a group of candidate agent execution sequences either you have agents in parallel and serial or you have other conditions and then it will execute and rank the success of those trajectories by some metric F1 scores token efficiency whatever you have entropy whatever and if we have the good things and the bad things the things that work the trajectories that are rejected we can build an experience experience library. Remember in my last video we were talking here about self-arning memory structure. What a coincidence that we have now an experience library. Now all of this this orchestration here generates here semantic advantages with semantic disadvantages given here the particular topology. So we have now positive and negative trajectory. And this is beautiful. We can build now here even operations operations like let's add something, let's merge something or let's prune something is now one of the operation we can perform within this library. So if you want during the inference run this library acts now as a conditioned prior biasing here the orchestrators topology sampling. There is something in addition that I would like to notice here. The role aware prompt evolution targets here underperforming agents causing your trajectory failures. No, because as I told you some of the topologies how you combine agent in a particular way in particular form will not lead to a successful execution. So therefore you will have a buffer of failed trajectories for each agent and beautifully is you have the good things and the bad things. You can do a contrastive analysis to generate your prompt updates along two axis along the operational rules and the behavioral principles. And now the crux is that you use now the frozen LLM to your benefit because what you say is you know what this is because the intelligence of the central LLM is frozen. It doesn't learn anything. It doesn't add knowledge. It doesn't add capacity to its knowledge body. So therefore what we have is kind of a implicit coolback library regularization. Why? Because the let's call it the intelligence of the LLM is frozen. This means it will not diverge too far away from its standard middle point. Yeah. So all the updated policy and we'll show you how we do a policy update remains within a certain interval around the original model stable support scheme. It will not diverge off extreme into the distance. So okay let's go with this an implicit go regularization by the frozen central LLM. So let's say okay if we have to do this and if the global AI corporation do not publish new AI models like Google did beautifully with German 4 and even put an Apache 2 open license on it. But if we have those organizations that are a little bit afraid of their IPO performance and do not publish new EI LLMs. So let's go here with the harness around the agents and let's see how we can optimize them further. So this loss landscape where we that we use here but if we have frozen closed weight LLMs no this loss landscape is now non- differentiable in our mathematical sense no why because look what we have here on this outer sphere we have API calls here to the supercomput then we have prompt syntax we have architectural routing problems we have different data pipelines a standard rag pipeline and and and end. How we combine all of this with a database cluster array, how we extract this, how we retrieve this, how we rerank all of this. You see, we have so many possibilities here in the optimization of the outer shelf. So, let's do this. Now in my very last video when we talked to you about EI designing its own memory with the auto research claw based on open core and we called this new methodology omni memory. We show that when a benchmark score is low the pipeline diagnoses here the semantic failure. The semantic failure can be formalized. Hey the prompt formulation caused some verbose outputs that destroyed here the exact match F1 metric. This omnim rewrites now the Python code or if you want the system code to enforce some strict constraints and you say hey you know what this is great and now what we have in Hera when we have a multihop reasoning task that fails the system doesn't just lower the probability of the output tokens no it really traces the failure to a specific agent node a single node is identified in the complete topology of our network of our graph structure and it [snorts] will formulate hopefully a natural language rule like the retriever agent failed because it didn't extract the temporal constraint in the correct way and it will patch so our frozen LLM the intelligence here of our core LLM will patch now the agent prompt so that this mistake is compensated do you notice if we have both of this system omni memory operates at a design time. So any scientist writes here, Python codebase, figures out how to construct here in the best possible way some vector databases and will establish you the optimal hard-coded prompt rappers. Yeah. what we talk about now operates at runtime and once it's deployed the system dynamically alters here its own computation graph of its multi- aent system per query based on the episodic memory of what failed seconds or maybe days ago if it has also the memory access. So this is now interesting a design time optimization and a runtime optimization and now you know exactly why I've chosen these two publication and I've showed you at first omnim and today I show you her if we would combine this complexity I mean this is just it goes beyond both papers this is my personal sort imagine we can combine this and you build an enterprise AI agent but first you would use the omnim auto research pipeline to discover here the optimal data ingestion chunking strategy ies then you would go here and let the EI system find the perfect vector database configuration for your particular set of domain specific data structure and then you will let also the find the best configuration of base system prompts all of this let's call it an offline optimization but once you have done this and optimized the omni memory now I would wrap those components in the hero orchestrator so that when our new agent or multi- aent system encounters now a normal multihop user query like I have a problem in theoretical physics or mathematics. It will now dynamically spin up parallel reasoning path check its semantic experience library maybe have done something in theoretical physics already and we do have some knowledge about it and it will patch its own sub agent prompt on the fly in an online adaptation. So this is how I imagine how I could combine omni and error because both systems demonstrate that an intelligent search over discrete programs or discrete prompts is now the new paradigm and this is guarded by the LLM reflection and and unfortunately this is true. This is far more sample efficient than we would go and go to the frozen LLM, unfreeze it and learn the core LLM, new knowledge, new skills, new ideas. So whatever we would have in a supervised fine tetuning and then reinforcement learning phase of a complex AI system regarding here a complex system engineering and agent topology optimization we just leave the LLM frozen and we let here the outer sphere find in a search not in a learned reflection but in an intelligent search over those program space here find the optim optimal solution. It is not as elegant as we would use an AI and have manifested the intelligence of the eye and have a self-arning AI core system. Now we just search here for all the possibilities in the sphere around a frozen LLM. Yes, you can do this. Yes, it is a path forward but it is not as elegant as really come up with a new intelligent LLM itself. So therefore for me here is kind of an agile project manager around the core LLM around the core intelligence managing just a team of LLM agents. I don't know if you're familiar with this agile idea but this is absolutely what Hera looks to me. Now this is a screenshot here from the paper Hera by the authors. So yes I know April 1st but honestly I swear to you this is a screenshot. So what we have we have agents. We have an agent that is a query decomposer reduces my complex theoretical physics query in five simple to understand queries. Then we have the retriever. We're talking about rag. So retrieves database internet your documents whatever. Then we have an answer generator. Then we have a query rewriter. Then we have an evidence selector agent. Then we have an reflect agent. And then we have a context validator agent. And then we have an agent that finally concludes everything that we come up with. So from this idea of this is here the the mixture or the spectrum of agents has decided here. Okay. A query comes in and my super intelligence orchestrator agent says now okay for this complex query from this person. I try out now different agent sequences. At first I have a pink biggie. This is the query rewriter. Then we have a mouse. Mouse is the retriever. Okay. And then we have a cow. Cow is the query rewriter. Okay. We try this out. And then we have another configuration and we add more and more agents and we just have a look we have a a particular metric F1 for examples and we just have a look what is the best performance and what are the reflection what are the inside the agent can deduct. So I would say this is pure trial and error and if you have some pre-existing knowledge you know for a particular complexity maybe this configuration will work or for a real heavy interwoven complexity maybe you really need here cow chicken whatever agent great it is important to understand if you look at this from a mathematical lens that you're operating now over structured action space of agent's gamma the orchestrator performs a group based optimization at the level of a global strategy. So given our query guess what Q it samples now a group of candidate agent sequences yeah our sequences. So each sequence is executing by invoking the corresponding agents producing now a particular trajectory towel which is subsequently evaluated to obtain here a reward in a natural language representation. Wow. Okay. And then the orchestrator is prompted to articulate the reasons and we all depend here on the intelligent of our orchestrator agent for a relative success or a relative failure given my particular job across the group producing here a set of as I told you natural language insights and the insights are capital I and this semantic now advantages encode now our structured knowledge about effective reasoning strategies about agent interaction like this sequence and failure mode. Effectively replacing our numerical gradients that we can calculate and have back propagation and everything with some human interpretable and compositional signals given the eye can understand those signals and really makes composite natural language string out of this. So I just have showed you here the very first the top here of the graph. So then as I told you we do have an experience library over time we build it up or maybe you already have it if you work in the domain of mathematics or the physics and then comes the time here now as I told you second optimization is not only for the topology but also for the prompt for the role aware prompt evolution maybe you know this here for DSPI or whatever so you have a particular note and then you have prompt variants that are produced I think this the standard we don't have to really to talk about this but as you see this is here interesting and the orchestrator AI has now possible star configuration a tree configuration a complex graph interaction topology and it's simply trying out all the possible combinations to find you the best configuration for my particular task now if you want to be a little bit more on the mathematical sides we can have a more formal framing of the idea But now I just wanted to give you the main idea. No, that you don't think, hey, this is complex. No, it's not complex at all. Quite contrary, this is a step back. We say goodbye to mathematics and we say hello natural English formulation and now everything depends on the intelligent of the eye. But if you want to have this framework mapped onto a mathematical topology, let's do this. So dual level evolution multi- aent framework we go here for the topology and for the prompt optimization of course we have knowledge intensive task Q denote here distribution over the queries Y is here the answer space D is here the corpus providing supporting information we are talking about rack system so we formulate here the whole system as this tupil and you say my goodness what do we have here okay let's start is our central orchestrator agent. Great. N is the set of specialized agent. E is our experience library. S is here the state space. A is here the action space. on each agent n of the set n i is abstracted here as a tupil where we have our policy pi our prompt and our sequence our trajectory towel and the action space includes now both careful the reasoning operations and the tool invocations. So this is nice but this opens up that there is an error propagation. So careful if you have maybe a problem here with a reasoning operation you can have an error propagation into tool invocations. So we have to make sure this is not happening and then of course we have a transition dynamics and yeah our trajectories are familiar with. So given now we have a query Q the main intelligence the orchestrator agent produces now a multi- aent interaction topology conditioning on the experience library and hopefully we have their configuration that our orchestrator can say okay for this particular problem in theoretical physics I know here a topology that worked last time beautiful so we specify now a particular subset of agents the topology their execution order sequential or parallel and all the dependencies is and all the data sources that we need and all the particular data pipelines and so on and the agents execute then this according here to our interaction topology producing therefore a trajectory tower and the final answer y is synthesized from the information that is now aggregated along the trajectory typically based on the terminal state of the system. Beautiful. So what was the goal? The goal is here to jointly optimize again as I told you here the orchestration policy our pi tater now here our primary orchestrator agents and the rule specific prompts as I've shown you here in the screenshot of the paper with the piggy and the cow and the chicken and whatever while keeping the underlying llm frozen this was here in my image here the core llm is frozen and we just have the harness here the surrounding code optimization topology optimization, multi- aent configuration and so on. Yeah, policy optimization if you want. This is here the mathematics for the policy optimization. We have a policy evaluation and then a policy improvement that is then incorporated into the library. I think this is self-explanatory but just that you have it here. Now what is interesting one element I would like to draw your attention is the topology mutation that the authors describe. They say if something goes wrong, we have a secret weapon. So if you have a prompt evolution and this improves the agent level behavior, you might encounter persistent failures and this persistent failure also you have a prompt optimization indicates that you have some structural deficiencies here in your topology. Maybe you're using here the wrong parallel or sequential combination or maybe an agent is completely unsuited here for a particular task whatever. So they build in now a topology mutation where error performs now this topology mutation if a trajectory consistently fails. If the metric is 10 times close to zero system understands oops something is happening here. And here now starts to explore alternative structures by either replacing here understanding which agent caused the problem replacing this failed agent n with an alternative agent or augmenting now the topology with additional agents. So either you reduce or you add something to your topology yielding candidate topologies that are now hopefully successful. Hm interesting approach but okay now what are the results now after all this building up you can be sure the authors will present to us here in the last line in the last two lines here hera either they go with a QN314B model or a very old-fashioned llama 3 please stop using llama 3 models this is just doesn't exist anymore here in modern AI research and they show you it outperforms here the other simple advanced track system. Now you might say looking at the advanced track system, hey there are some systems missing. Yeah. Well, this is the set that the artist decided to benchmark against full stop. So but you get it. Yeah. Okay. So you see Hera with a 14 billion barometer model is here really outperforming here the other classical advanced rack system. Beautiful. I want to show you an example. Now the authors of the paper present to us here as a a case and since it's almost Eastern they come here up with the question is the exact reason known for the crucifixion of the biblical figure who was presented at the temple in Jerusalem and we do have immediately the query rewriter and the query rewriter splits this up into a question and says number one what is the cause of the crucifixion of the biblical figure who was presented at the temple and is number one. So the cause of the crucifixion known exactly and then we have the retriever and we go to the internet or to the Bible and then we have an answer generator and then we bring everything together into the conclude agent and the answer is no. And if you are a little bit familiar with the Bible you would say I'm not really understanding what's happening here. Well this is here interesting and this is one of the features here of Hera. If we have causal multihop question, Hera handles this by and this is kind of a positive thing also explicitly separating the retrieval of of causal evidence from the pure reasoning over uncertainty and this is beautiful because we reduce the complexity what the AI has to do step by step. So at first the I system identifies the target entity let's call this target entity Jesus if you are a Roman Catholic and we'll retrieve the relevant historical and biblical sources from the internet describing here the crucifixion and the contributing factors and the history and everything about it and the interpretation by all the different churches and whatever. Then the reasoning module will evaluate the retrieved evidence, check for consistency, check for logic, check for whatever accounting here for also ambiguity and contested interpretations to determine whether the absolute exact cause is definitely known and completely agreed upon by the complete community. Now by maintaining here the serial dependency aware workflow error tries to ensure that we will minimize the hallucination of the system because it could be that the system does not have enough data to come to a simple conclusion and therefore the AI agent will have a tendency to hallucinate the result or hallucinate a step or some conditions. So therefore the first step is reasoning our hops that built here on grounded evidence paper you find in the internet but you know in the internet you find paper that are in accordance with the Roman Catholic church and you find paper that are not at all in accordance here with the official interpretation by the church. So you find a lot of different interpretation. So therefore minimizing your hallucination or multiple interpretation by the eye is not a simple task and I think this is not of the best examples you can choose to clearly show here the functional differentiation of Hera between and what I think is a good idea the experience grounded pure retrieval task and then if we have all the retrieval maybe you want to have a reranking and maybe you want to have some logical checks and whatever and then we have the adaptive causal reasoning task which allows here in the separation Hera hopefully to robustly handle here even multihop question where the cause and the certainty must be inferred sequentially interesting interpretation but you see the pro and the cons of the system now on a personal note going beyond the paper that I presented to you I think the ceiling of Hara's performance is strictly bound here by the intelligence of the frozen base LLM at the core and particular its capability of this LLM for complex reasoning the capability of this LLM for in context learning so hopefully in the pre-training data there was some explicit domain knowledge for this particular case and a strict adherence to the structured output like JSON and this means if the core LL LM fails at its structured operation and don't get me wrong this harness around the central LLM is beautifully constructed and everything but it is a structured operation and if the LLM fails on this execution the whole system will suffer in Hera the LLM is asked to play three highly complex roles at first it is the orchestrator agent who reads the query and look at past experience and output a perfect valid JSON map of a multi- aent topological workflow. Second, the reflector. It must look at the logs of successful and failed agent in the past and right now based on the experience a highly precise logical explanation in natural English of why this team in the past of agents failed. And if it finds not all the data, it might tend to hallucinate the reason and then it will store pure garbage in the experience library and we will have an error that propagates across all levels of the experience library. And third, the executor agent. Yeah, if the specialized agent, the retriever, the validator, they can be smaller LLMs. No, but if they are not smart enough for this particular domain, for the level of my complexity query, if they are not smart enough to follow here even their own prompts, the final answer will be wrong. If you want, have a look at the evaluation and the listening to the reasoning traces of gamma 4 in one of my last videos and you will immediately understand what I mean here. So if the base LLM the frozen LLM lacks a strong instruction falling capabilities error will experience some compounding error propagation across the complete agent harness. A bad plan will lead to a bad execution which leads to a bad reflection a bad understanding which will poison here the experience library completely. So therefore, although this is a very beautiful paper, think about it. It all depends by the complexity that we build here on the AI harnessing sphere around our agent. And if there is one mistake here in the natural language interpretation, not in the code structure, not in the mathematical optimization, not in the numerical reward functions, but just in the pure understanding how something is formulated and inside the topological performance of our multi- aent system in natural language and this is not in 100% understood by the LLM. M every actions by the LLM will lead here do a nonoptimization of our topology and therefore to have not here a verifiable reward system either numerical or mathematically verifiable or verifiable by code execution but all depends on this beautiful human language and let's say we talk about I don't know a biotechnical thing or a purely theoretical physics thing maybe here the understanding of the LLM in the natural language let's say English is not as perfect as code or as numerical solver so therefore I would be a little bit careful if we transform everything back to a natural language we say okay let's forget about all the mathematical optimization and let's just solve everything in human language with AI system and AI hopefully understands everything perfectly for further optimization of a graph topology and I'm not absolutely sure that this is the case what do you think would be great to hear from you otherwise would be great to see you in my next you.