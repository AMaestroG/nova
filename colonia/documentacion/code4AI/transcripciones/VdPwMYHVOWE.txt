# AI Evolve Itself HyEvo
# URL: https://www.youtube.com/watch?v=VdPwMYHVOWE
# Canal: @code4AI
# Segmentos: 581

Hello community. So great that you are back. Today we talk about hybrid evolution a self-arning hybrid reasoning manifold for the next generation of AI models. In my last video we were talking about hyper agents that were based on a Daring girdle machine and MTO tried to optimize here a hyper structure on it. Well, as you see, we do have here this topic of self evolving, self-evolution, self-constructing AI is here really the main topic here this week. And we have a new study. This is here March 20, 2026. Self- evolving hybrid Atlantic workflows for efficient reasoning here. This is by the East China Normal University, Behang University, Shanghai University, FAN University. Now they saying let's build a system that can autonomously construct some new architectures in our EI agent system by integrating now some LLM nodes for the semantic reasoning and some code nodes for the deterministic execution. So the idea is simple. There's a lot of things that an L&M a large language model cannot do like mathematics. No. So therefore we just go we have a code node. We go to a Python environment C++ have there the mathematical calculation and then feed it back into the reasoning of our large language model. Let's do this. But what we want is that is not a linear structure or star structure. We want now this as an optimization problem that an AI should solve. So we want to eliminate the manual topology design of this particular agentic structure. So we no longing need to manually craft some lang chain or autogen or whatever you were working when you were younger where you had to decide where to place the critique LLM or the generator LLM or the orchestrating LLM or whatever. Now we have EI. So why not let an AI system discover here the optimal mathematically calculated optimal computational graph for this particular job for your target data set and careful this is a highly specific target data set configuration. Now first idea was simple no separate here the task complexity in general. So we are offloading all the deterministic operation like mathematic execution or data writing here from a probabilistic LLM generation to our code nodes. You see here the little hollow effect. So we build our topology here some manifold here that have some specific node characteristics and this is yet the complete hybrid evolution framework and I think let's have a look. This is here the screenshot here from the study and the others tell us let's initialize the workflow. So we have to initialize here a particular population. Now if we start with zero we have none. No. So simple. Then we have here if you want the start of the self- evvolving loop. So we select now hopefully there is some start configuration here. We have some basic workflow and I will give you an example in about 5 minutes time. Let's call this the parent workflow that we selected. Now for this particular looping for this run. Then we have some execution logs. we will have a top workflow and we have some other diverse workflow that might be not as beautiful and precise. So what we're going to build now is here a looping structure that is a self- evvolving framework that will autonomously quotation mark construct some heterogeneous agentic workflow by separating out the linguistic reasoning task here from the deterministic task like calculate here the probability So this first refraction phase here is simply we have multiple agents of course no and a meta agent performs now and you see this also in my last videos we have a meta agent and a working agent a comparative analysis to diagnose the shortcomings of let's say the primary parent here by examining its failure logs its execution failure logs if you have a python just say okay if you are python code just run tell me are you working yes or Great. Then the second step is it simultaneously draws insight from here this population. Therefore we need to have some kind of a start population and we look here at the high performing example and we call this the graph top the top performing graph structure and some other graphs that are yeah diverse. Yeah it's okay. It's it's not bad at all because the system has now to distinguish between the effective and ineffective pattern in the graph topology itself. So our meter agent or super agent identifies now specific structural bottlenecks. Hopefully this is an LLM that is able to do this and then it would formulate here in English in a natural language a diagnosis. says okay listen this happened this happened I I understand that this is here a mistake was made because the format was incorrect or the mathematical um method was incorrect whatever and then guided by this diagnostic gradient that we have now the next generation phase synthesizes now here a new workflow a graph new a new heterogeneous workflow and maybe in this workflow you have now two LM nodes And maybe you have three coding nodes where each code node has you a a specific task now a subtask. So you see we have now the meter agent here that defines here now both things the topology of the graph and the node parameters itself to resolve here the identify the issues here from the error locks. Now as you understand we have this reflect and act phase. So this is our classical way but of course it all depends here on the power of yeah reasoning of our ma agent and then yeah simple we have a sandbox where we run some evaluation you have performance execution time cost metric whatever yeah sandbox protocol great but then comes the interesting point so we manage now the wolf workflows using now a multi island strategy and was a little bit surprised what do you mean with a multi the island strategy here and that's some nice idea they say you know each island here of a solution maintains now a local population and we are back to the start where I told you we are working now with populations of two components you have here the historical set that stores here all valid workflows everything and then we have here you want to know which of those valid workflows were the best for time were the best for cost were the test for reasoning capability. You get it? So what you build? You build a structured, they call it an elite archive, A4 archive that discretizes now the phenotypic space into some grid cells. For example, here you see here the note count or here some ratio. So however you want here to build the internal structure of your elite archive you immediately see if you want to optimize here for a particular metric this would be here the dimension the axis to optimize on. So using here workflow compaxity they went here in the study for the total note count. Now is the total note count really the perfect metric for a workflow complexity? Maybe maybe not. And then they went for the reasoning density and this is now defined by the orus as the proportion of LLM nodes. So you see there's a lot of optimization space. How you can build much better let's see island structures. Now why we do this? This mechanism ensured that the archive retains only the highest performing solution for each specific structural configuration. As I told you, inference time, cost, complexity, reasoning structure, you got it. I know some of you asked me in my last videos, hey, why we stay here with a normal graph structure? Why don't we go to hyperraph? So, we have hyperraph transform architectures that I showed you here 10 minutes ago or two minutes ago, how to code and how to build it. Well, the artist decided to go here with a classical graph structure. Okay, now you know if you're a subscriber of my channel as I showed you how to build this. Now we have topological distortion and whatever we have here some click expansion that we have to take care. So hyper edge and hyperraph can connect to any number oh number of nodes. Sorry for the typo and of course this would be a much more advanced methodology but yeah you can do it if you want. Now let's come back to the original paper. So this preprint formulates here if you want an autonomous agentic workflow generation but not as a prompt engineering task from the context and content but we go now here as a multi-objective neural architectural search where we say okay let's think we have a space of all possible combination architectural neural architectural configuration that are theoretical possible it's a huge space no and now we have simply to search for the optimal configuration for our specific task for our specific query for our specific validation data set. The authors call this space the hybrid search space omega. Great. So what do we have? We have as I told you the meta agent the overarching let's call it a super llm the boss however you want to call it. It does not solve the actual user queries. Its only job is to analyze logs, design the optimal workflow given its particular pre-training and training data and then write the prompts and the Python scripts that go into the specific node configuration into the topology of the graph. Now who is doing the real work? The node LLMs, the workers, no LLM instances that live inside the workflow that the architect built and it has three LLM nodes like say the classical, you know, the planner, the summarizer, the reviewer. Those are three separate calls here to an LLM during the execution. Now [clears throat] the interesting thing is that if we have now a node space we the see this as a union of two distinct generative subspaces and we have here the vlm those are the llm nodes where we have here the model the instruction yeah the temperature and whatever hyperparameters you have and then we have here the code subspace here those are the code nodes where we have synthesized source code and IO No signature the output is strictly a function of the input. Got it? So we built here a directed asyclic graph our DAG here a classical graph G t theta and now we have at first look at all the complexity and then find the perfect path. So we are now encountered with an optimization a mathematical optimization objective function. Here I have a particular video on AI mathematics if you want. But here we simply maximize the aggregated reward function in the simplest possible way. Look we have here the SQ the performance accuracy then we have here the token cost and then we have here the latency. So you see in this simple idea this is here what we optimize the system for. As I told you this multi- island elite evolution strategy is here more or less this really interesting thing because a standard evolutionary research algorithm in an unconstrained graph space that is huge leads to a catastrophic mode collapse because yeah we have to restrict it somehow no so the population isn't just a list of the best graph no our here explicitly bent here by the structural complexity as I showed you the note foot count or the reasoning density. So we limit here our space. This ensures that the structurally diverse architecture will survive. So we have what they call here a map elite structures. Okay. And then this is interesting. Elements between islands can migrate. So the system maintains let's say k 20 50 100 isolated populations. And every delta migration iteration islands can exchange their best solution. So you can have a further optimization. You say okay let's have 10 islands and now you look at the top 10 structures and you say can I combine this? Can I further optimize this? So we immediately understand what we're talking about. Then comes the moment of context sampling. You're familiar with this. The agent is fed as I told you to get a parent graph gore parent. its failure logs parent and a top performing example and some diverse structural effort just here the graph diverse at the beginning of the video I showed you this here in the visualization and then we have a diagnostic string here a reflection here by pi data explaining why the parent graph failed on specific edges give some explanation and then of course we want that the system understands the error log comes up with a beautiful solution we'll synthesize the code for the offspring graph new will say okay those are the amount of of code uh nodes that we need of LLM nodes that we need and it will build the perfect topology for a given complexity of your task yeah we talked about sandbox evaluation beautiful of course they start here to be fast on a small partition inside the sandbox to catch it the syntax error here was a massive logical failure and only if it clears here the first baseline threshold then they expose it here to the full data set evaluation Right. Let's show you an example. So this is a very simple my example. This is just to give you the basic insights. Let's say we have my query. Extract a mathematical equation from a messy text. Let's say a PDF format and solve those mathematical equation. So meta agent doesn't try to be clever yet. It creates a seed workflow consisting exactly of one LLM node and the task is read the text and output here an answer. Now the system now takes this node workflow runs it on a validation set let's say 100 mathematical problems that are of the same complexity and of the same mathematical depth like here the mathematical equation in my text careful and then it collects you the data it says out of this 100 problems I give it here in the validation set it got 60 right cost $2 in token 45 seconds but it completely failed on complex mathematical operations Of course, we have an LLM node, not a code LLM. So, step three is now says, okay, MA agent is looking here at the execution log, the token cost, the latency, and the RS, and it says, okay, the LLM node keeps hallucinating some mathematical formulas here. So therefore, this is just a waste of time if you just go with a large language. Well, we have to go and now we have to build a new workflow graph. And in this workflow graph, guess what? We have now a code node for the Python execution. Congratulation. So new structure created by the meta agent. Llm node one extract the equation to pure text. Code node then python eval to calculate the mathematical formulas and llm node 2 format the final response. So you see in this simple example, this is the way to go. Now carefully you may ask what is specific on this methodology? Well, it doesn't just loop endlessly on that one piece of code. No, like coding standard assistance what we have. No, because it is focused here on building the best graph structure optimizing here the topology of your LLM nodes and of your code specific nodes. There is a point where the meta agent might look here at the failure might say okay maybe this py evaluation node is too fragile here. So let's change here the whole architecture again and it will invent a new topology. It will invent a new loop between new members between new node structures. You see this is here not just here a pure code optimization or a context prompt optimization. This is here really the architectural way how we build multi- aent systems. Wait. And here we have a screenshot here from the study and you see exactly what we went through here in this visualization with our island. We start here with a initialized workflow. Then we have our island one and then we run through all the optimization topics. What are the results here? You have it for the different uh benchmark that we have average here. Let's have a look at the last column here. They go here from chain of sword complex chain of sort and then we have um a flow and the last line is here the new hybrid evolution. So as you see this new model is here really outperforming here beautifully but careful think about why does it outperform there are some limitation and maybe the orcas did not really point this out so allow me to have my thoughts about this. If you simply prompt an LLM to design an optimal multi- aent graph, it will absolutely reorgetate here the topologies that it saw during its training the pre-training data set. No, and it goes with what it knows, what it has been trained on. Chain of sword, basic actor critique loops, react patterns. No, LLM's raw generation capability is absolutely bounded by the latent space of its pre-training. And again and I know everybody was hoping this that suddenly AI wakes up and is now super intelligent but up until today it did not happen. So it cannot zero shot or invent here a radical new topology a new paradigm whatever and the orus again try to kind of deal with this limitation by telling us now yeah it's not a mutation engine I'm so sorry this is just here a combinatorial multitude how we combine here different nodes and what complexities we can achieve on the different levels and we have an Okay, let's call it an evolutionary search routine here with our elites. Yeah, but if you compare to biology, you immediately understand this has nothing to do here with a mutation structure. And I already had this point here in my last video. If you want to have a better understanding, I would highly recommend you have a look at the Py code because more and more I really start to love this Py code because it gives you really here the logic not in the text sometime the order in the text are focusing on one part and not really mention all the complexity of the second part. But here you have it. Absolutely. So maybe this helps you a little bit in understand here the flow the flow chart here of this system but I want to tell you what are the limitations. We do have definitely an overfitting to the target distribution. No, because it optimizes here this graph topology specifically for a given data set. And as I showed you the mathematical benchmark structures, no. And if the evolutionary search process that we have at the beginning that takes time discovers now a highly specific Python string parsing rule to extract the the correct mathematical answer of a very specific topic that code node is now if you want really baked into the workflow but if you deploy this workflow now into the real world you say okay let's forget about the evaluation procedures we know for three years let's go out in the real world and a user asked now a slightly different formatted mathematical question. This code node will instantly crash as the or tell us the generated workflow is highly fitted here to the prompt distribution of the validation set. So you train it quotation mark here on a very specific given data set your singular mathematical benchmark predefined and then it works for this. The generalization is not there because think about it. We are talking about a topology optimization. It is not a topology generalization. This might be the topic of another video. We do have a massive single point of failure. And this of course is the meta agent's coding ability. No, if it rewrites the code and if it rewrites the topology. So I hope you use maybe for the coding ability an opus 4.6 or whatever you like. Okay. For writing, debugging, connecting code notes based on our logs, you have to have a powerful code structure. And when you read the paper, you got a feeling that the authors kind of brag about how cheap the inference cost is. No, the final generated workflows. And when you run them at inference time, this workflow, it's so cheap. It's so beautiful. It's so much faster. And they're absolutely right. It is faster. But you know, maybe there's one question we forgot to ask here. What are the training costs for this topology? No, because to find this optimal graph for this particular benchmark, but this mathematical benchmark for this verifiable benchmark, the framework runs 40 iteration across multiple islands, constantly evaluating the generated workflow using a validation set, using some large LLM, generating the Python code, running it, finding here the syntax error, reflecting thousands of prompts, and you got the idea. Maybe it's my mistake. I think the paper completely omits here the dollar cost and the computer hours required to run here the self evolving loop at the very first place before we start the inference because we have to train this particular system to really achieve a good topological graph structure. So yeah I could not find the cost or the hours for the training. So therefore it is no surprise that doing now the inference run this might be 19 times cheaper than other systems. But remember the other systems were using massive LLM to do things like hey check if this string is a valid JSON format. Yeah. And checking here JSON with an LLM costs real money and may take I don't know 3 5 seconds depending on the complexity. No. Now if we do it here simply checking here an expression with a threeline Python script this costs I don't know let's say zero dollars and it takes I don't know 1 millisecond or less no so you see it's it's not really apples to apples no it is it's a it's a new methodology but careful no because there is a chance that this new hybrid evolution it trades here the inference latency and it is fast for a massive upfront search compute for a very particular task structure and you risk of creating some real rigid overfitting pipelines. So there is a benefit and they show the benefit but careful in order to come up with this topological optimized graph structure that separates your code LLMs and pure LLM nodes there's a lot of training that goes into a particular benchmark optimization okay anyway I hope you had a little bit of fun it is kind of a self- evvolving system absolutely so another understanding how self- evolving can play out if we look now here at an optimization for the pure workflow structure. I hope you had a little bit of fun. I hope you had some new information, some new insights. It would be great to see you in my next