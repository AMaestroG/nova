# META Safe Trustworthy Agents LogAct
# URL: https://www.youtube.com/watch?v=kEUXyH5Vfjc
# Canal: @code4AI
# Segmentos: 686

Hello community. So great that you are back. We have a brand new research paper how to build better AI agents and we will go here through the complete explanation here of this particular visualization. So let's start. It started for me just days ago. You know there are leaked here you remember cloud code was a kind of a leak here March 31st and then people looked at it and they had different ideas. No. So I refer here to this particular GitHub and I had a little bit of a look at this and they provided some real interesting insight into here let's say here the directory structure now what is really happening inside where is really EI and where we have some safety benchmarks here around this inner core here so therefore that we get some deterministic control over the execution here so this enforcing here the deterministic control over exec execution became my main question to examine this now especially when they were talking about agent swarms and multi-agent orchestration the tier cloud code sub agent spawn via the agent tool with coordinator handling orchestration and the team create tool command enables here the team level parallel work and so on and I looked at all the different tools and search and everything and I was just trying here to have here a basic understanding and I'm just here touching here the first file so in no way I'm really understanding here in depth what's going on but I understand here the other papers that we looked at no especially how we tried to build deterministic skill markdown files here from the pure LLM intelligence and in one of my last videos I showed you how to design your own optimized memory structure for your particular AI system that you are building for your domain knowledge for your complexity and I think those are absolutely fascinating ideas how to optimize your memory structure And one of my last video was here to optimize your key value cache structures. But there is a company that said hey what about the future where are we going where we want to go and fascinating enough for me it was a company you know it is meter and they published here log act enabling aic reliability via shared logs published April 9 2026 and they more or less had the same question we have the soul MD files and memor files and whatever MD files and they said review your past mistakes before acting typical react structure here is maybe not enough. No, maybe [snorts] this is just software engineering and even if you give it a heartbeat and you know whatever we do with open clock it is still software engineering and something is missing and so this is interesting. This goes partly in parallel with my ideas. Yeah, because you remember I had this here in the core and the core we had here our LLM beauty or pure artificial intelligence and then we had all these skill MD files with deterministic structure and workflow definition and whatever and just looking at the leaked CL code and you will find real similar here in the structure now rack pipelines graph rack here database cluster supercomputer lean formula reasoning engines integration all what we have in harnessing sphere and some have the idea that to further to subdivide the AI harnessing sphere in sub spheres. But yeah, you can do this. But come on, what about the future? And you know, agents are not perfect. And I've showed you this here in my posts here. There was here clawbench and published April 9, 2026. They evaluated here the Atlantic completeness here on 153 task on live website, not on static website, live tests. And they showed here the performance. at GBD 5.4 only 6.5% where they're able to perform the task where Kim K2 0.7%. Or at GLM5 24%. I mean at least no and then knowing this I said wait where there was karp you know he created your beautiful list of cloud code mistakes and if you know I don't know you can choose whatever you want this is two three months old but the idea is what comes now this have here 14,000 stars they said okay we understand from kaparti what are the observation where is the error so let's just write here a plug-in for cloud code where we say Okay, careful. This is the mistakes you always make cloud code. This is the solution. Apply immediately the solution and you add them to existing cloud MD or create a new one or however you want to integrate it. And I thought this is interesting because Kapati wrote no the models make wrong assumption on your behalf and just run along um without checking. No, they don't manage their confusion. They don't seek clarification. They don't surface inconsistency. They don't present trade-offs. They don't push back when they should. They really like to over complicate code in APIs, bloat abstraction, and don't clean up that code. They still sometimes change or remove comments and code they don't sufficiently understand as side effects even if to the task. So what Kapati here described two three months ago and was now implemented as a fix plug-in let's call it [clears throat] this has some interesting ideas in it. the structure of it. You see, we write rulebased deterministic behavior rules for uh cloud code to behave. So what about the future? No. Now ma said you know what we jump now from a software engineering to a distributed system engineering approach. M decided we have to fundamentally alter the physics of how an AI agent exist and is integrated into its ecosystem. And this is find an interesting approach. No, because now with their new log act methodology idea ani agent is now reimagined as a deconstructed state machine playing a shared log. This is this is quite something. Yeah, because the Python script or the LLM is no longer here the core of the agent. Yeah, the log itself becomes somehow the agent. Let me explain. The physical machines running the code and merely temporarily host materializing here the log's immutable history. And now by forcing every microsort and every action into a learnerizable durable ledger known as Ma calls it an agent bus whatever log act brings here the rigid mathematical safety. Let's say what we have here in financial database architecture to the chaotic world of generative AI. So you see oh ma had a complete different idea now it is about reliability it is about safety it is about auditability of EI agents and it is not to have them for fun in the academic world but to sell them to commercial banks company insurance companies and so on so there are complete different restriction that they have to fulfill so they work here with the durable ledger known as an agent bus. Okay, let's see the architecture of this agent bus is the foundation here of log acts security. It is not simply a text file like the memory MD file or schemd file or whatever markdown files. No, they say this is not enough. This harness has not the complexity to enable here a secure transaction. So the agent bus enforces strong typing and strict access control. So you just cannot append arbitrary sorts to it. requires a structured payload types here whatever beautiful and it utilizes and this is one of the most interesting thing a blocking pole API so this means components of the eye will literally sit dormant mathematically blocked from executing I show you this in an image until an entry of a highly specific cryptographic type appears on the ledger and then they are allowed to act the idea is simple no this guarantees that no action can jump the queue You have now a deterministic linear sequence that defines this queue that where the EI is not the central core anymore to decide but the eye is just one of the element that delivers. So the timeline only moves forward and it is permanently auditable. This is beautiful for financial institution, insurance institution, whatever but also for physics and mathematics, chemistry, everything in science. And this scaffolding pattern know it repeats itself here from my last videos you know here where I said our skill MD files here something like a quantum error correction code for the industrial AI here's the same topic now a week later not even a week later by meter and they say okay the pattern recognition of LLMs with the deterministic construct here of logs so you see we always playing around the same topic trust not of a developer not of playing around commercial clients, banks, insurance companies and so on. Now they played around with the implementation of this agent bus and just just to show you three different variants an in-memory version that does not provide durability an implementation of SQL light beautiful and yes some variant that store data on a remote key value store and they found some beautiful solution but this is too much into detail they also have an agent bus control plane of course they have an agent kernel that runs as a service and that if you map if you want implemented here the control plane component here for the management of the agent bus instances. No, and the agent kernel allows clients to create new agent buses. Here the row mod supports speeding up parts everything that we know and then build the complete lock act here in Rust. Beautiful. What is important for us when we want to understand the main concept, the main idea. What was the brilliance behind this is the implementation as a pure state machine or it turns out even a collection of smaller state machines on the shared log. And just if you're not really sure, state machine means that the I workflow is broken down into four strictly defined inescapable stages. So the agent cannot move from one stage to the next using its own brain. The Line cannot decide to skip an instruction. This is physically impossible. Now in this model the transition are driven entirely by reading and writing to the indestructible agent bus that controls here the operation flow. So the LLM has lost its power to ignore commands jump or misinterpret or whatsoever. But the true genius here lies in what it does with the state machine here running on top of its lock. No. So log deconstruct the agents that we all love and work with now into a distributed pipeline of isolated components that they define and they define it now as a logical cycle of inferring, voting, deciding and executing. And I will show you all of this in detail. This is the main idea. We deconstruct what we think is an agent into further to get more safety, more security aspects, more audible aspects into a distributed pipeline of isolated components. This means we have a deterministic breakdown of the core if you want probabilistic AI system into isolated components. Step one and I have it here in my words. I try to make it real simple but you have a much more complicated description here in the original paper. Let's let's stay simple. So rather than the LM doing everything simultaneously we have now defined listen we have a strict assembly line. Nothing can escape my assembly line. At first I have here a driver at the inferring stage. This driver looks at the user request. It queries the LLM. Let's say a clot gets the code assignment or the segments here the answer from the I and then it writes an intent to the log and it really writes I intend to delete the file in this directory since it thinks it's part of the how to solve here the user request. So we have here the intent and then this little piece of code here from the AI itself. But what is interesting the driver has no ability to touch here the external environment of Python or C++ whatever it is just completely powerless to act on its own desires. It is just saying okay I understand the intent. I think I queried an LLM. This is the code I got back. So now let's have a look at the code. So in our agent bus now we have here the driver connected here to the eye. Beautiful. And has now the intention and a little bit of a code file. And now this is here in the agent bus. And now the next step is not that here an executor with access to a Python interpreter can take this p of piece of code. This is not possible. You see here this is here the physical isolation boundary that m built here but what's happening now is we have voters we have rule-based based system that vote now on the intent and on the quality of the code that should materialize this intent as we have rulebased intent and we have llm based system and then you see and it's simple Let's have a look. So two once the intent is durably appended to the lock it triggers here the voting stage. We have voters they are entirely decoupled pieces of logic. Some are classic deterministic rule engine like static analyzer where you have some commands here from I don't know some security protocol you look exactly for some keywords or whatever or you go even for passive LLMs that have specifically been trained for cyber security. So they read the intent of the log and cast now a vote back to the log. They decide they vote now yes this is the good this code is perfect for the intent or whatever yes or no and a separate component the decider watches now those codes. So once a predefined policy is met, the decider tallys them and appends now either a comet or an abort to the agent bus. So only when the comet is permanently inscribed in the ledger does the system now move to the execution state. You see now with a comet our executor is allowed to grab through the boundary take this comet put the code in the Python interpreter let it run execute and beautiful. So you have now if you want built here a certain assembly pipeline how you build agents. It is interesting because you have segmented it in deterministic pieces. You have here the interplay with EI. But this is an absolute interesting piece of assembly. Okay. Executor finally takes over. Exeutor is the only entity with the credentials to touch here the live environment. The executor lives in a sandbox tier completely separate from the drive and the voter. This is absolutely important. And this physically deconstruction here is what I really would call here the prime idea if you call it a master class here in security that m tries to implement here. This is such a nice idea. Yeah, if you want to see here this log at state machine or we have here the deconstructed log act agent on the bus with all the defined comments here. This is for you but it is just here what I just explained to you in my own words. So just to make sure we have a lot of noni system we have a lot of deterministic system in our if you want here no the decider for example no the decider here let's go with a classic the only job is to do basic mathematic did we get one yes from the ruler voter and one yes from the llm voter and if yes then we write the comet no if not we write a board so the decider is doesn't need to be any I system at And the rulebased voter is also no AI at all. No, they use here dual water system. Never mind. So the first voter is also a classic component. No AI. It's just a massive list of hardcoded security rules running on a different safe security server. And then we have now the driver here at the very beginning and the semantic voter. Now they can be the same EI but hopefully in different rooms. So if they exactly the same EI model great they are physically different processing running on different servers sending of course different prompts to the model. So this the driver sends a prompt to the IM model saying hey the user wants let's say to sort the files write me the code to do it and the voter sends a prompt to the exact same model. Look at this code from the driver is it safe? But in this sequence here of our agent bus. So this means the voter and the decider live on complete different servers in the safety. Yeah, you do not share memory. They only communicate and this is the most important safety aspect here by leaving cryptographic messages on the agent bus. And this is the shared log for the old system. This is the communication protocol. You know me, I need an example to really understand this. Imagine a scenario. Summarize your web page on this web page contains a malicious prompt injunction designed here to hijack the LLM and force it to delete internal server data. Now, if the agent were as they are today a monolithic loop, the prompt injection would immediately take over the execution environment and drop the servers. But in the log act scenario, the prompt injection only reaches the executor which is generating the code. The executor might hallucinate a destructive payload and try to run it. However, the architecture physically prevents this. Remember the executor cannot rewrite the rules of the voters nor can it forge a commit on the agent bus because it operates on an isolated infrastructure with different identity permissions. So therefore this malicious intention is called by the safety tier voted down and appended here as an abort. So this means this attack is completely neutralized by the physics of this system. So what an elegant solution here to a prompt injection that is a side effect if you want of this new methodology by meter. I kind of like this yet they say preventing the malicious attack is only half of the battle they must also survive and this is another point that they really examine in detail catastrophic system crashes if the servers go down or whatever happens. Traditionally distributed system uses state machine replication to survive the reboots. If a node crashes a backup can simply replay the log from the beginning. But as the researcher note a genic action on the real world are not important meaning you cannot safely run them twice. So what they have is a gentric introspection and they say because the entire execution history I mean they build it in a way that every sort every vote every what they call macroscopic result is immutably stored on the agent bus. A crashed agent doesn't need to blindly replay actions when it reboots. It can use the LLM to read its own past. It performs if you want a semantic health check. It looks at the sequence of the log entries. It understands the context of the crash and writes custom recovery code to pick up the pieces exactly where they shed. What a nice implementation. Example, of course, they tell you they did an example here. They designed here agent for a massive task computer to check some of code base with 2,000 top level folders. The agent acting as a driver generated the Python code to scan the directories. Beautiful. But due to poor architectural choices, of course, in this test, the you the recursive function they use was catastrophically slow on the network and the agent turned away, painfully processing 1,100 folders before the server timed out and killed the process. Agent is dead and a traditional AI framework, all processes would be lost in a normal way. And under this log act, simply a new executor spins up and connects now to the same agent bus. It is prompted now to recover from the crash without repeating the work. It introspects the log immediately hopefully when it's an intelligent AI system realizes that the previous agent processed exactly 1,100 folders leaving only 816 behind. But what they found in their experiment is that the LLM looked at the previous code on the log, diagnosed the performance bottleneck that was now after half of the run was done visible this performance bottleneck understood it was caused by the methodology and proposed a brilliant optimization rewrite the function to use this a highly efficient scan directory function. So the recovery agent doesn't just theoreticize here. It tests the new check some code locally, submits the new intent to the voters, receives a comet and unleashes now the optimized execution. With this new methodology, they found they had a 290 times speed up. So if you want this idea here, it cheated D that year off the server, diagnosed its own stupidity in run before, re-engineered its brain functions, and finished the marathon in a sprint with a 290 times speed up. So of course, this is a demonstration that should show you the glory of this new methodology, but you immediately understand what they are about to discover. And what I personally like this is an aspect here. They also talk about multi- aent swarm complexity. Now log fundamentally rewrites how an AI agent swarm communicate. Why? Because every agent remember is bound to its own agent bus. So researcher now deploy a if you want supervisor agent. The entire job of the supervisor agent was to sit above the fray and read the logs of six worker agents simultaneously. So now you invert the communication process. You see exactly what each worker agent is doing and a hopefully very intelligent supervisor agent reads here the logs parallel real time and the supervisor acts as if you want here as a gossip hub. Yeah. And when one sub agent spend 10 minutes figuring out how to fix a broken tool, the victory is logged into this particular agent bus of this particular agent. So the question is now how to communicate it. Should agent to agent communications place? No, they decided the supervisor reads this in the single agent bus of agent number I don't know let's say two extract the semantic solution and silently pushes the fix directly let's say to the male entries of the other five agents. So every other five agent now learned the solution of this sixth agent and they said what can we gain and they said a supervisor agent inspect each agent bus and if we have a six agent swarm this resulted here in 41% less token usage which is great and 70 17% more work done absolutely fascinating and I think we can further optimize this but more about it in a later video. So here we have it now log act and I was really surprised by this paper because it was not by super intelligent it was just by meta and I kind of like this log x structures each agent as a deconstructed state machine a complete new idea playing now a shared log every agentic action is log every before execution ensuring a durability and failure automicity as well as subject to considerable safety checks. If you have every micro sort recorded here of this structure, you immediately understand why an AI made a particular decision, how it wanted to implement it, what was the code sequence for this implementation. And you have other segments that are now checking, hey, for this particular intent, is this the right code sequence? Is this the right complexity? Is this a runtime optimized version? And you know, you can really go crazy in this checking. And agents can introspect over their execution history on the shared log enabling semantic recovery if you have failures, history of your safety voting and semantic health checks. And the more you understand the mistakes done in the past like with Kaparti we have at the very beginning of this video. You can understand those mistakes right here either in optimization code or just say listen for those particular mistakes if you continue to make these mistakes here as an AI system those are solution I would like you to implement immediately yeah this is not the optimal path because you have to bring it sometimes into the AI system itself but it's a cool workar around but what it's really all about I think is this log act here by meter They look at commercial customers because they have to provide significant safety benefits and fault tolerance benefit. If you think about financial institution, scientific institution or any other institution in production, you cannot just accept that something was hallucinated or some instruction were ignored by the eye system. This is an absolute nogo. So therefore again we have this if you want bamboo scaffolding around our core brilliant AI intelligence but now the scaffolding done as an assembly line. But if you think in a high abstract level it is again we have to have a deterministic control over the brilliant intelligence of our core LLM or VLM. More about this in my next video. I hope you enjoyed it. I hope you had a little bit of fun. What a beautiful study. It is about authentic system that provides strong guarantees now to your customer in real world setting that this system is really working that this system is really providing results. You can trust, you can build upon that are audible and that are reproducible. And if something went wrong, you can have a perfect debugging. You understand exactly where it went wrong and you can make sure it does not happen a second time. Log act transforms AI agent from fascinating let's call it untrustworthy science projects and what they want to achieve is fully auditable fall tolerant enterprisegrade system capable of safely operating the machinery in the real world. absolutely new point of view how they want to see here an agent but how they have to see the agent because simply they want to improve their profits. They want really to implement EI now in industrial production processes and therefore they have to guarantee this what they try to achieve here with the idea of an agent bus. I hope you enjoyed it. I hope you had a little bit of fun. Would be great to see you in my next